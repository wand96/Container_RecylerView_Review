<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.container_recyclerview_review">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Container_RecyclerView_Review">
        <activity android:name=".MainActivity"
            android:exported="true">
<!--            exporeted는 다른 애플리케이션 구성요소로 Activity를 시작할 수 있는지를 설정
                다른 앱에서 Activity를 시작할 수 있으면 true, 없으면 false로 설정
                intent필터를 사용할 때는 false로 설정하면 안됨-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>